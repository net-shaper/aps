<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/incoming"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="63dp"
        android:text="@string/hello_world" />

    <Button
        android:id="@+id/startserver"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/incoming"
        android:layout_marginTop="80dp"
        android:layout_toLeftOf="@+id/incoming"
        android:onClick="connect"
        android:text="@string/service_start" />

    <Button
        android:id="@+id/endserver"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/startserver"
        android:layout_alignBottom="@+id/startserver"
        android:layout_toRightOf="@+id/incoming"
        android:onClick="disconnect"
        android:text="@string/service_stop" />

</RelativeLayout>